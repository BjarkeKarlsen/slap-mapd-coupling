---
paths:
  - "examples/**"
---

# Example script numbering

`examples/NN_name.py` files are numbered by build milestone (see
`docs/implementation_phases.md`), not by topic. Keep new examples in that
sequence rather than renaming or reordering existing ones.
