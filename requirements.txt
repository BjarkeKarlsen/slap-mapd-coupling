pydantic>=2.6
ray[rllib]>=2.9.0
torch>=2.0.0
gymnasium>=0.29.0
numpy>=1.24.0
networkx>=3.0
matplotlib>=3.7.0
pyyaml>=6.0
click>=8.0.0

# Optional (matches pyproject.toml's [viz] extra): only needed for the
# interactive pygame renderer, not for headless training/CI.
# pygame>=2.5.0
