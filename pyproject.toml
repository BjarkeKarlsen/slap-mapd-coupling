[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "slap-mapd-coupling"
version = "0.1.0"
authors = [
    { name = "Bjarke Karlsen" }
]
description = "Coupled storage-assignment (SLAP) / multi-agent pickup-and-delivery (MAPD) simulation and RL controllers for a warehouse graph."
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.6",
    "ray[rllib]>=2.9.0",
    "torch>=2.0.0",
    "gymnasium>=0.29.0",
    "numpy>=1.24.0",
    "networkx>=3.0",
    "matplotlib>=3.7.0",
    "pyyaml>=6.0",
    "click>=8.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "black>=23.0.0",
    "flake8>=5.0.0",
    "mypy>=0.990",
]
viz = [
    "pygame>=2.5.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
# 85% is a floor with headroom, not a target -- current coverage is ~93%.
# Raise it as controllers/environment/training grow real tests, don't
# lower it to make a red build pass.
addopts = "-v --cov=src --cov-report=html --cov-report=term-missing --cov-fail-under=85"

[tool.black]
line-length = 100
target-version = ['py310']

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true

[[tool.mypy.overrides]]
# networkx ships no inline types and there's no maintained third-party
# stub package pinned for this networkx version; ignoring missing imports
# for it specifically (not project-wide) is the standard mypy pattern for
# an untyped dependency.
module = "networkx.*"
ignore_missing_imports = true

[project.scripts]
slap-mapd = "slap_mapd_coupling.main:cli"
