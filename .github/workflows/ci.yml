name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: pip

      - name: Install package (dev + viz extras)
        run: pip install -e ".[dev,viz]"

      - name: Run tests
        env:
          # pygame needs a display; the "dummy" SDL driver lets the
          # pygame_renderer tests run headless in CI (no real window).
          SDL_VIDEODRIVER: dummy
        run: pytest

      - name: Compile-check every source and example file
        run: python -m py_compile $(find src examples -name "*.py")

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: pip

      - name: Install package (dev + viz extras -- mypy needs pygame installed
          to check viz/pygame_renderer.py's import, same as the test job)
        run: pip install -e ".[dev,viz]"

      - name: black --check
        run: black --check --diff src examples tests

      - name: flake8
        run: flake8 src examples tests

      - name: mypy
        run: mypy src
